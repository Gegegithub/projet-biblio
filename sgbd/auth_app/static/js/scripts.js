document.querySelector('button').addEventListener('click', function () {
    const searchTerm = document.querySelector('input').value;
    alert(`Recherche en cours pour: ${searchTerm}`);
    // Implémente ici la logique de recherche
});
